service : {
    "get": (key: vec nat8) -> (opt vec nat8);
    "put": (key: vec nat8, value: vec nat8) -> ();
    "reset": (nat) -> () oneway;
    "needs_data_buckets": () -> (nat);
    "add_data_buckets": (data_canister_ids: vec text) -> ();
    "lookup_data_bucket_for_get": (key: vec nat8) -> (opt text);
    "lookup_data_bucket_for_put": (key: vec nat8) -> (opt text);
}